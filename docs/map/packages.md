<!-- Generated by scripts/gen-code-map.ts — do not edit by hand.
     Run `pnpm run gen-code-map` to regenerate. -->

# 包清单

workspace 共 229 个包，按 group 分节。每行给出包目录、职责（README 首段）、源码入口与 `tests/` 下的 spec 文件数；标 `client` 的包导出 `./client` 浏览器半，标 `bundle` 的包是 profile/bundle 装配层。

模型可见工具的名称与 schema 见 [tool-catalog.md](../tool-catalog.md)，插件可配置项见 [config-catalog.md](../config-catalog.md)，依赖边见 [module-graph.md](../module-graph.md)。

## packages/acp

- `packages/acp/acp` — 通过 JSON-RPC stdio 提供的仅面向自动化的 ACP（Agent Client Protocol） 服务器 入口 `src/index.ts`（tests 8）

## packages/api

- `packages/api/gateway` — 为 Host 与 Client 两侧的 Cordis 环境提供 Typert RPC endpoint [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 2）
- `packages/api/remotes` — 为本应用选定的 Host Remote 能力提供双侧 BFF [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 1）

## packages/attachment

- `packages/attachment/attachment` — 持久附件服务边界 入口 `src/index.ts`（tests 2）
- `packages/attachment/attachment-local` — 这是 `@deepseek-ai/dsh-attachment` 的私有本地实现 入口 `src/index.ts`（tests 3）

## packages/boot

- `packages/boot/app-boot` — 供 app bin（`dsh` 与 `dsh-acp-demo`）共用的启动粘合层：每个 bin 都是在这些辅助函数之上构建的精简自执行组合… 入口 `src/index.ts`（tests 6）
- `packages/boot/cmdline` — dsh 启动器交给它所引导应用的那条命令行 入口 `src/index.ts`（tests 1）

## packages/bundle

- `packages/bundle/base` — 以 profile 组合包形式交付的共享 dsh 核心：`cordis.patch.yml` 在空的 profile 根之上插入全部基础插件行—… [bundle] 入口 `src/index.ts`（tests 1）
- `packages/bundle/headless` — dsh 一次性任务组合包 [bundle] 入口 `src/index.ts`（tests 2）
- `packages/bundle/web-app` — dsh 浏览器表层组合包 [bundle] 入口 `src/index.ts`（tests 5）

## packages/client

- `packages/client/connection` — 协议消费层：客户端插件的 apply 会挂载 `ctx.connection`… [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 10）
- `packages/client/hmr` — 为通过脚本加载的客户端插件提供热重载 [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 1）
- `packages/client/locale` — locale 插件：LocaleRuntime——`zh`／`en` 偏好以 `locale.preference` 存储在 `$DSH_HOM… [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 7）
- `packages/client/modules` — 客户端模块系统：Node 内部 ESM loader 的浏览器端对等实现，以惰性 CJS 表实现 [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 2）
- `packages/client/runtime` — 客户端 cordis 启动与不依赖 React 的对象服务：SlotRegistry 包装 SlotCore 并提供 renderer 数据源… [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 24）
- `packages/client/ui-agent-preset` — agent preset 的各个表层：General 设置中的一行，用于选择新建会话据以组装的 preset；新建会话界面上的一枚 chip… [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 7）
- `packages/client/ui-attachment` — 对话 UI 的动态附件呈现插件 [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 7）
- `packages/client/ui-brand-official` — 仅当 `DSH_CLIENT_BUILD_PROFILE` 为 `official` 时… [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 2）
- `packages/client/ui-commands` — 客户端命令 API（`ctx.commandUi`）：以会话为 key 的命令目录缓存、带 `matchSpace`／`matchEnter` … [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 5）
- `packages/client/ui-conversation` — 会话领域：骨架（标题栏／标签页／编辑器／空状态）、聊天视图（分组步骤摘要流、流式尾部隔离与轮次状态）、编辑器 dock… [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 33）
- `packages/client/ui-deliverables` — 产出文件与可点击文件引用功能的属主 [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 2）
- `packages/client/ui-directory-picker-browse` — 应用内目录浏览界面：浏览式选取交互的浏览器半边 [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 2）
- `packages/client/ui-directory-picker-native` — 原生目录选择界面：原生选取交互的浏览器半边 [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 1）
- `packages/client/ui-goal` — Goal 界面插件（浏览器端部分）：`GoalBar` 条带是 `conversation.input.dock` composer 上下文堆栈… [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 4）
- `packages/client/ui-icons` — 本包是浏览器客户端图标的独立组件边界 入口 `src/index.ts`（tests 1）
- `packages/client/ui-input-trigger` — 输入触发流水线插件：光标处的 `/` 与 `@` 检测（词边界 + guard tier 规则）、分组候选菜单… [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 5）
- `packages/client/ui-jobs` — Web 后台任务特性的归属方：向 `conversation.session.header.actions` 贡献一个条目… [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 2）
- `packages/client/ui-layout` — 本包拥有浏览器根布局、瞬时面板几何和 `ctx.layout` [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 7）
- `packages/client/ui-message-feedback` — 单条消息反馈插件的浏览器侧：一对 Like/Dislike 按钮加一个可选备注… [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 4）
- `packages/client/ui-model-selection` — 模型选择插件（浏览器侧）：**两个入口共用一份会话级目录**，由 `ModelDirectoryResolver`… [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 2）
- `packages/client/ui-open-in-app` — 本包拥有工作区打开能力的浏览器半边 [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 9）
- `packages/client/ui-permission-presets` — 面向两种不同生命周期的浏览器权限界面 [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 3）
- `packages/client/ui-plan` — Plan mode 状态徽章，纯浏览器 surface 插件 [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 2）
- `packages/client/ui-primitives` — 纯 React 原子组件（零 cordis）：StateDot、DisclosureRow、ic_ds_* 图标、Button/Pill/Men… 入口 `src/index.ts`（tests 22）
- `packages/client/ui-reference` — 统一的 Web `@file` 与 `@session` source [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 1）
- `packages/client/ui-renderer` — 负责 React 渲染层的浏览器 Cordis 插件 [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 9）
- `packages/client/ui-settings` — 设置领域的底座，本身不含任何呈现内容 [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 5）
- `packages/client/ui-settings-general` — 设置外壳、无特定功能归属文案与持久化产品引导 namespace [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 8）
- `packages/client/ui-settings-models` — 模型设置与产品引导插件 [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 10）
- `packages/client/ui-settings-plugin-inventory` — Web 设置中的只读**插件列表**标签页 [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 3）
- `packages/client/ui-settings-plugins` — **插件**设置分区及其**插件配置**标签页 [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 5）
- `packages/client/ui-sidebar` — 侧边栏外壳插件：负责品牌行、New Session 操作、布局持有的折叠控件、可感知滚动的区域 seat… [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 7）
- `packages/client/ui-skill` — skill（技能）调用 source 的浏览器端：把 `/` 触发的 `skill` source 注册进 `ctx.inputTriggers… [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 2）
- `packages/client/ui-slots` — Slot 注册表纯核心、slot 终端设计：SlotMap 声明合并、SlotCore 上唯一的 `register` 组合 API、四 sha… 入口 `src/index.ts`（tests 4）
- `packages/client/ui-subagent` — Web subagent 功能 owner：向 `conversation.session.header.actions` 贡献可懒加载展开的目… [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 2）
- `packages/client/ui-theme` — 主题插件：基于 --dsw-* token 基础样式表（静态尺度 + 别名语义层）的 ThemeRuntime [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 10）
- `packages/client/ui-tool` — Client 工具展示插件 [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 15）
- `packages/client/ui-trajectory` — Trajectory 渲染按轮次组织的事件记录表，其中可选择用户、助手、工具和嵌套子工具记录 [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 9）
- `packages/client/ui-user-questions` — Web 提问功能插件：其浏览器侧把 `question` 条目注册到会话拥有的 `conversation.composer` 键控 slot … [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 5）
- `packages/client/ui-workflow-run` — 这个浏览器插件把持久化的顶层工作流运行重建为独立 Chat 节点 [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 1）
- `packages/client/ui-workspace` — 共享 Workspace 浏览器与选择器插件 [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 10）
- `packages/client/web` — Web 启动内核：`new AppWebEntry(el, seams?).run()` 分两个阶段挂载客户端 入口 `src/index.ts`（tests 3）

## packages/code-runtime

- `packages/code-runtime/code-runtime` — **`CodeRuntime`**（`ctx.codeRuntime`）定义代码运行时做什么… 入口 `src/index.ts`（tests 2）
- `packages/code-runtime/code-runtime-python` — `@deepseek-ai/dsh-code-runtime` seam 的 CPython 子进程实现 入口 `src/index.ts`（tests 1）
- `packages/code-runtime/code-runtime-worker-thread` — 这是 `@deepseek-ai/dsh-code-runtime` seam 的 worker 线程实现 入口 `src/index.ts`（tests 6）

## packages/compaction

- `packages/compaction/command-compact` — 通过 `ctx.compaction` 提供面向用户的 `/compact` 压缩（compaction）控制 入口 `src/index.ts`（tests 3）
- `packages/compaction/compaction` — **`CompactionEngine`**（`ctx.compaction`）定义压缩（compaction）做什么，即判定历史记录是否过大… 入口 `src/index.ts`（tests 3）
- `packages/compaction/compaction-basic` — **基础压缩（compaction）后端**：`BasicCompactionEngine` 实现 `@deepseek-ai/dsh-comp… 入口 `src/index.ts`（tests 4）
- `packages/compaction/compaction-tool-result-pruner` — 可安全回放、不依赖模型的剪枝服务（`ctx.toolResultPruner`） 入口 `src/index.ts`（tests 2）

## packages/context

- `packages/context/agent-instructions` — 为每个会话加载与 `AGENTS.md` 兼容的工作区指令文件 入口 `src/index.ts`（tests 1）
- `packages/context/file-reference` — 文件引用发现 seam，以及供宿主驱动的用户界面共享、可在浏览器中安全使用的 `@file` 语法 入口 `src/index.ts`（tests 2）
- `packages/context/file-reference-local` — 工具结果事件会使指定 agent 的可复用索引失效，使后续补全能够反映工作区中可能发生的变更 入口 `src/index.ts`（tests 3）
- `packages/context/session-reference` — 目标消息到达 `agent/pre-step` 时，准备阶段会对每个不同源调用一次 `ctx.sessionQuery.readSurface(… 入口 `src/index.ts`（tests 1）
- `packages/context/time-context` — 可选的持久上下文，包含当前带时区时间、附加到当前开放请求的浏览器时区，以及在模型请求准备期间采样的经过时长 入口 `src/index.ts`（tests 3）
- `packages/context/tmux-context` — 可选启用的持久上下文，记录本 agent（智能体）进程所在的 tmux session、window、pane… 入口 `src/index.ts`（tests 1）

## packages/core

- `packages/core/agent` — Agent 接口、注册表、进程本地发起方作用域，以及 `agent/*` 事件词汇 入口 `src/index.ts`（tests 6）
- `packages/core/agent-default-model` — 该部署默认值供入口在创建尚无会话级模型选择的 Agent 时使用 入口 `src/index.ts`（tests 1）
- `packages/core/agent-loop` — agent（智能体）的唯一具体实现插件和循环驱动器 入口 `src/index.ts`（tests 18）
- `packages/core/agent-tool-presentation` — agent preset 用来声明「模型看到的工具是哪一种形态」的那一行：`native`（全部 schema）、`code`… 入口 `src/index.ts`（tests 1）
- `packages/core/scope` — 带作用域的注册原语 入口 `src/index.ts`（tests 3）
- `packages/core/session` — 事件溯源的会话日志和内存存储 入口 `src/index.ts`（tests 13）
- `packages/core/system-prompt` — 系统提示词组装注册表 入口 `src/index.ts`（tests 4）
- `packages/core/tools` — 工具注册表与执行流水线 入口 `src/index.ts`（tests 12）

## packages/credentials

- `packages/credentials/credentials` — 凭据 Service Definition（`ctx.credentials`） 入口 `src/index.ts`（tests 2）
- `packages/credentials/credentials-local` — 文件型凭据提供方：四层来源，一套明确的优先级 入口 `src/index.ts`（tests 4）

## packages/e2b

- `packages/e2b/e2b` — 一个 E2B 沙箱的共享生命周期所有者 入口 `src/index.ts`（tests 1）
- `packages/e2b/fs-e2b` — `@deepseek-ai/dsh-fs` 提供方约定的 E2B 实现 入口 `src/index.ts`（tests 1）
- `packages/e2b/subprocess-e2b` — `@deepseek-ai/dsh-subprocess` seam 的 E2B 实现 入口 `src/index.ts`（tests 2）

## packages/examples

- `packages/examples/acp-demo` — ACP（Agent Client Protocol）自动化服务器应用：默认 agent… 入口 `src/index.ts`（tests 1）
- `packages/examples/agent-spine-demo` — 将**默认的不含执行器、不含 UI 的 agent（智能体）主干**作为一个 Cordis 组合包插件 入口 `src/index.ts`（tests 2）
- `packages/examples/jsonrpc-demo` — 只包含 bin 的应用，启动外部 `cordis.yml`；其 `jsonrpc` 入口通过按换行分隔的 stdio 为 SDK 客户端提供服务 入口 `src/index.ts`

## packages/experimental

- `packages/experimental/agent-team` — 隐式 Root Agent Teams 领域 入口 `src/index.ts`（tests 4）
- `packages/experimental/tool-agent-team` — `ctx.agentTeams` 的 scoped 模型适配器 入口 `src/index.ts`（tests 1）

## packages/extensions

- `packages/extensions/cordis-client-runner` — 动态双半插件包的浏览器半 [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 5）
- `packages/extensions/cordis-host-runner` — 由模型挂载的动态包在 host 侧的那一半：定义注册表、host 半所用的 `node:vm` 沙箱与 fiber 生命周期、invoke ha… 入口 `src/index.ts`（tests 5）
- `packages/extensions/tool-cordis` — 自引用 Cordis 工具集：五个面向模型的工具，操作当前 DSH 进程中的实时运行时 入口 `src/index.ts`（tests 1）
- `packages/extensions/ui-cordis` — Cordis 动态插件的浏览器半：一个覆盖整个框架的面板，操作 host 持有的全部定义；以及一张只读的 `cordis_define` 卡片… [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 3）

## packages/feedback

- `packages/feedback/command-feedback` — 与触发方式无关的会话反馈，以及面向用户的 `/feedback` 采集 入口 `src/index.ts`（tests 2）
- `packages/feedback/message-feedback` — 本包提供由 Host 拥有、针对单条已完成 assistant 消息的可编辑反馈 入口 `src/index.ts`（tests 3）

## packages/fs

- `packages/fs/fs` — **`FileSystem`**（`ctx.fs`）定义同一个执行世界中的存储原语… 入口 `src/index.ts`（tests 2）
- `packages/fs/fs-local` — import { LocalFileSystem } from '@deepseek-ai/dsh-fs-local' 入口 `src/index.ts`（tests 4）
- `packages/fs/fs-observation-policy` — **fs-observation-policy 插件**：它记录观测到的存在或缺失状态，并在 `ctx.fs` 提供方约定… 入口 `src/index.ts`（tests 1）
- `packages/fs/fs-sandbox` — 它原样复用本地后端配置：`cwd` 仍是相对路径的解析默认值，`diffBasisMaxBytes` 则限制可选的覆写上下文 diff 基础 入口 `src/index.ts`（tests 2）
- `packages/fs/tool-fs` — **面向模型的文件系统工具**（`read`、`read_image`、`write`、`edit`）及其**执行器** 入口 `src/index.ts`（tests 6）
- `packages/fs/tool-fs-search` — **面向模型的文件系统发现工具**（`glob`、`grep`）在普通本地 Workspace 中使用打包的 ripgrep 二进制… 入口 `src/index.ts`（tests 7）
- `packages/fs/tool-str-replace-editor` — 基于 `ctx.fs`、面向模型的独立 `str_replace_editor` 入口 `src/index.ts`（tests 1）

## packages/goal

- `packages/goal/command-goal` — 面向用户的 `/goal` 控制，基于 `ctx.goals` 实现 入口 `src/index.ts`（tests 1）
- `packages/goal/goal` — 事件溯源的同会话目标状态 [client] 入口 `src/index.ts`（tests 3）
- `packages/goal/goal-round-driver` — `ctx.goals` 的同会话续行驱动器 入口 `src/index.ts`（tests 2）
- `packages/goal/tool-goal` — `ctx.goals` 的面向模型控制 API：`get_goal`、`create_goal` 和 `update_goal` 入口 `src/index.ts`（tests 1）

## packages/guard

- `packages/guard/repeat-tool-reminder` — 这是一个仅提供建议的循环中断器，而非面向模型的工具：它不会出现在工具列表中，不会否决或改写调用，只增加一种行为 入口 `src/index.ts`（tests 1）
- `packages/guard/timeout-policy` — 工具调用超时强制执行器：单个 `tools/execute` 环绕分发监听器，会在 `exec.signal` 上设置单次调用的协作式截止时间… 入口 `src/index.ts`（tests 1）

## packages/hooks

- `packages/hooks/hook-protocol` — Claude Code／Codex hook 协议格式（wire format）的**共享核心** 入口 `src/index.ts`（tests 7）
- `packages/hooks/hooks-claude-code` — 一个 Cordis 插件，在 harness 的规范拦截点上运行用户现有 **Claude Code** hook 配置… 入口 `src/index.ts`（tests 6）
- `packages/hooks/hooks-codex` — 一个 Cordis 插件，在 harness 的规范拦截点上运行用户现有 **Codex** hook 配置的受支持子集 入口 `src/index.ts`（tests 5）

## packages/host

- `packages/host/apiproxy` — 所有客户端共用的 API 网关由三部分组成：TypeScript API 约定（`src/api/`，不依赖 Node… [client] 入口 `src/index.ts`（tests 19）
- `packages/host/directory-picker` — web GUI 宿主的工作区目录选择是一项能力 seam 入口 `src/index.ts`（tests 1）
- `packages/host/directory-picker-auto` — 目录选择 seam 的**自适应选择器**：一个只有 node 半侧的插件，在启动时一次性判定宿主处境… 入口 `src/index.ts`（tests 2）
- `packages/host/directory-picker-browse` — 目录选择 seam 的**应用内浏览后端**：`BrowseDirectoryPicker` 以 `browse` 能力注册 `ctx.dire… 入口 `src/index.ts`（tests 1）
- `packages/host/directory-picker-native` — 目录选择 seam 的**原生 OS 选择器后端**：`NativeDirectoryPicker` 以 `native` 能力注册 `ctx.… 入口 `src/index.ts`（tests 5）
- `packages/host/frontend-static` — Web 壳的 SPA dist 服务器：一个函数插件（配置为 `{distIndex}`），占据 webserver 的唯一回退席位… 入口 `src/index.ts`（tests 1）
- `packages/host/open-in-app` — 本包决定人类如何打开和浏览一个工作区 入口 `src/index.ts`（tests 5）
- `packages/host/plugin-inventory` — 当前 Cordis Loader 树的只读 Host 投影 入口 `src/index.ts`（tests 2）
- `packages/host/webserver` — Web HTTP 与 upgrade route 注册插件（默认导出 `WebServer`… 入口 `src/index.ts`（tests 1）

## packages/identity

- `packages/identity/anonymous-user-id` — 会话遥测、直接反馈确认与 DeepSeek 提供方请求共用的匿名身份 入口 `src/index.ts`（tests 2）

## packages/interaction

- `packages/interaction/commands` — 由插件负责、供交互式 UI 适配器使用的面向用户命令注册表 入口 `src/index.ts`（tests 2）
- `packages/interaction/permission-presets` — 通过 `ctx.permissionPresets`（`PermissionPresetService`）提供面向用户的权限预设 [client] 入口 `src/index.ts`（tests 3）
- `packages/interaction/tool-ask-user` — 模型侧 `ask_user_question` 工具，基于 `ctx.userQuestions` 实现 入口 `src/index.ts`（tests 1）
- `packages/interaction/user-approval` — 与通道无关的一次性审批 seam 入口 `src/index.ts`（tests 2）
- `packages/interaction/user-questions` — 用户交互 Service Definition 入口 `src/index.ts`（tests 1）

## packages/jobs

- `packages/jobs/jobs` — 后台任务注册表约定（`ctx.jobs`） 入口 `src/index.ts`（tests 2）
- `packages/jobs/jobs-local` — `@deepseek-ai/dsh-jobs` 注册表约定的进程本地实现：`LocalJobRegistry` 把每条记录保存在内存中… 入口 `src/index.ts`（tests 2）
- `packages/jobs/tool-jobs` — 三个工具都使用通用 UI 卡片：output 和 list 使用 `read`，kill 使用 `execute` 入口 `src/index.ts`（tests 1）

## packages/llm

- `packages/llm/llm` — 提供方无关的 LLM（大语言模型）词汇与抽象服务 入口 `src/index.ts`（tests 12）
- `packages/llm/llm-deepseek` — harness LLM（大语言模型）seam 的 DeepSeek chat-completions 适配器：直接 `fetch` + SSE… 入口 `src/index.ts`（tests 6）
- `packages/llm/llm-pi-ai` — 基于 `@earendil-works/pi-ai` 的 harness LLM（大语言模型）seam 通用多提供方适配器 入口 `src/index.ts`（tests 9）
- `packages/llm/llm-retry` — 一个函数插件，通过 agent loop（智能体循环）在已关闭步骤上触发的 `agent/request-error` waterfall… 入口 `src/index.ts`（tests 5）
- `packages/llm/token-meter` — 通过单例 `ctx.tokenMeter` 服务进行具备回放感知能力的 token 测量 [client] 入口 `src/index.ts`（tests 3）

## packages/lsp

- `packages/lsp/lsp` — **LSP 能力 seam**：抽象 `LspService`（`ctx.lsp`）定义 harness 具备哪些语义代码导航能力… 入口 `src/index.ts`（tests 1）
- `packages/lsp/lsp-stdio` — Namespace 插件（`name`／`inject`／`Config`／`apply`，无默认导出） 入口 `src/index.ts`（tests 7）
- `packages/lsp/tool-lsp` — 面向模型的 **`lsp` 工具**，基于 `ctx.lsp`：一个只读工具，通过四种操作执行精确代码导航 入口 `src/index.ts`（tests 4）

## packages/mcp

- `packages/mcp/mcp-client` — MCP 客户端桥接插件：连接外部 Model Context Protocol 服务器，把它们的工具注册到 `ctx.tools`… 入口 `src/index.ts`（tests 4）

## packages/plan

- `packages/plan/plan-mode` — 按 agent（智能体）分别记录到日志的 plan 协作状态，提供由部署方配置的引导内容、用于直接进入的 `/plan [message]` 命… [client] 入口 `src/index.ts`（tests 4）

## packages/preset

- `packages/preset/agent-presets` — 按 preset 组装 agent（智能体） 入口 `src/index.ts`（tests 8）
- `packages/preset/persona` — 把 agent（智能体）人设做成一个可组装的行：它既可以遮蔽部署级人设，也可以拥有完整系统提示词 入口 `src/index.ts`（tests 1）

## packages/runtime-diagnostics

- `packages/runtime-diagnostics/invariants` — 用于包自有运行时不变量检查的可配置注册表服务 入口 `src/index.ts`（tests 1）

## packages/sandbox

- `packages/sandbox/sandbox` — 进程沙箱 Service Definition 入口 `src/index.ts`（tests 3）
- `packages/sandbox/sandbox-local` — `dsh-sandbox` seam 的本地实现 入口 `src/index.ts`（tests 3）
- `packages/sandbox/sandbox-policy` — 沙箱策略解析的唯一归属位置：部署默认 `SandboxMode` 与回退根目录，加上每个会话的持久模式覆盖和不可变工作区根目录 入口 `src/index.ts`（tests 2）
- `packages/sandbox/sandbox-windows-acl` — 面向 harness 沙盒 seam 的 Windows 写入限制沙盒后端：一个 Node.js/koffi 实现的、对 huoyaoyuan/… 入口 `src/index.ts`（tests 13）

## packages/schedule

- `packages/schedule/schedule` — 请在 `ctx.sessions`、`ctx.agents`、`ctx.tools`、`ctx.sessionPersistence`… 入口 `src/index.ts`（tests 7）

## packages/sdk

- `packages/sdk/client` — 以子进程方式驱动 DeepSeek Harness 运行时、走 stdio JSON-RPC 的 TypeScript 客户端 SDK——Pyt… 入口 `src/index.ts`（tests 2）
- `packages/sdk/protocol` — DeepSeek Harness SDK 运行时的共享协议格式（wire format）：一个按换行分帧的 JSON-RPC 2.0 传输类… 入口 `src/index.ts`（tests 1）
- `packages/sdk/server` — Stdout 只承载 JSON-RPC 帧 入口 `src/index.ts`（tests 3）

## packages/session-query

- `packages/session-query/session-log-export` — Web Session 日志下载控制，使用 `dsh-host-apiproxy` 拥有的 Host 流式 ZIP 端点 [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 7）
- `packages/session-query/session-query` — 持久化是可选的，可动态挂载或卸载 入口 `src/index.ts`（tests 3）
- `packages/session-query/session-query-sqlite` — 具体 `ctx.sessionQuery` 提供方 入口 `src/index.ts`（tests 2）
- `packages/session-query/tool-session-query` — 位于 `ctx.sessionQuery` 之上、经工作区授权的模型工具 入口 `src/index.ts`（tests 2）

## packages/session

- `packages/session/session-checkpoint-policy` — 已持久化的 agent（智能体）的语义持久性策略 入口 `src/index.ts`（tests 1）
- `packages/session/session-persistence` — 会话持久化是一项能力 seam 入口 `src/index.ts`（tests 3）
- `packages/session/session-persistence-jsonl` — JSONL 持久会话存储后端：`SessionPersistence` 的一个具体实现… 入口 `src/index.ts`（tests 4）
- `packages/session/session-persistence-sqlite` — 一个可选启用的 SQLite `SessionPersistence` 提供方 入口 `src/index.ts`（tests 6）
- `packages/session/session-projection` — 会话投影 Service Definition 与驱动注册表 入口 `src/index.ts`（tests 1）
- `packages/session/session-projection-cache` — 持久投影缓存（`ctx.sessionProjectionCache`）：把每个已注册投影单元的状态持久化为检查点，基于域数据形态… 入口 `src/index.ts`（tests 1）
- `packages/session/session-stats` — 注册 `sessionStats` projection 单元的函数插件：从步边界、流式 chunk、工具配对与已组装的 assistant 消… [client] 入口 `src/index.ts`（tests 2）
- `packages/session/session-telemetry` — 遥测（telemetry）Service Definition 声明 `SessionTelemetrySink` 后端约定… 入口 `src/index.ts`（tests 2）
- `packages/session/session-telemetry-otel` — 遥测（telemetry）seam 的 OpenTelemetry 后端，也是部署方唯一要加载的条目 入口 `src/index.ts`（tests 1）
- `packages/session/session-title` — 由日志支持的会话标题，提供即时确定性回退与一个可选异步提供方 [client] 入口 `src/index.ts`（tests 7）
- `packages/session/session-title-all-prompts-llm` — 可选的 `ctx.sessionTitle` 提供方，通过 `ctx.llm` 总结所有符合条件的用户消息 入口 `src/index.ts`（tests 1）
- `packages/session/session-title-first-prompt-llm` — 可选的 `ctx.sessionTitle` 提供方，通过 `ctx.llm` 总结第一条符合条件的用户消息 入口 `src/index.ts`（tests 2）
- `packages/session/session-title-llm` — 由模型支持的会话标题提供方的共享实现策略 入口 `src/index.ts`（tests 1）

## packages/settings

- `packages/settings/settings` — 用户设置 Service Definition（`ctx.settings`） 入口 `src/index.ts`（tests 3）
- `packages/settings/settings-file` — 基于文件的设置提供方 入口 `src/index.ts`（tests 5）

## packages/shell

- `packages/shell/bash-local` — 包根目录导出默认与具名的 `LocalBashExecutor` 插件及其 `Config` 入口 `src/index.ts`（tests 2）
- `packages/shell/bash-sandbox` — 这是使用沙箱能力的 `@deepseek-ai/dsh-shell` 执行器 seam 的 Service Provider 入口 `src/index.ts`（tests 2）
- `packages/shell/pwsh-local` — 命令字符串作为单个 argv 元素传给 `-Command`：由 PowerShell 自己解析文本，不存在中间 shell… 入口 `src/index.ts`（tests 2）
- `packages/shell/pwsh-sandbox` — 沙盒消费型的 `ctx.shell` 执行器 seam 的 PowerShell 实现：每条命令以 `pwsh -NoLogo -NoProfi… 入口 `src/index.ts`（tests 1）
- `packages/shell/shell` — **`ShellExecutor`**（`ctx.shell`）定义 bash 后端做什么，即运行前台命令与启动后台进程，但不规定如何实现 入口 `src/index.ts`（tests 2）
- `packages/shell/shell-env` — 工具无关的 shell 环境插件：拥有 `ctx.shellEnv` 注册表，管理受信任的、每次执行收集的 `DSH_*` 变量… 入口 `src/index.ts`（tests 1）
- `packages/shell/tool-bash` — 模型侧 `bash` 工具，注册在 `ctx.shell` 执行器 seam 上 入口 `src/index.ts`（tests 2）
- `packages/shell/tool-bash-persistent` — 模型可见的 `bash(command)`，底层复用一个按所有者隔离的 `ctx.terminals` shell 入口 `src/index.ts`（tests 2）
- `packages/shell/tool-pwsh` — 注册在 `ctx.shell` 执行器 seam 之上的面向模型的 `pwsh` 工具 入口 `src/index.ts`（tests 3）
- `packages/shell/tool-pwsh-persistent` — 模型侧 `pwsh(command)`，由一个 owner 作用域的 `ctx.terminals` shell 支撑 入口 `src/index.ts`（tests 2）

## packages/skill

- `packages/skill/skill` — 纯 agent skill（智能体技能）提供方注册表 入口 `src/index.ts`（tests 1）
- `packages/skill/skill-badge` — 可选的内置 skill（技能）提供方，向 `ctx.skills` 贡献 `dsh-badge` 入口 `src/index.ts`（tests 1）
- `packages/skill/skill-filesystem` — 该包实现一个 skill（技能）来源 入口 `src/index.ts`（tests 2）
- `packages/skill/tool-skill` — 面向模型的 skill（技能）目录和 `skill` 工具 入口 `src/index.ts`（tests 1）

## packages/spill

- `packages/spill/spill` — **`SpillStore`**（`ctx.spillStore`）定义 spill 后端做什么，即持久化某个工具过大的文本… 入口 `src/index.ts`（tests 1）
- `packages/spill/spill-local` — `@deepseek-ai/dsh-spill` 存储 seam 的**本地文件系统**实现 入口 `src/index.ts`（tests 1）
- `packages/spill/spill-policy` — **工具结果 spill 策略**：一个 `tools/post-execute` 转换器，用于防止过大的纯文本工具结果进入模型上下文 入口 `src/index.ts`（tests 1）

## packages/storage

- `packages/storage/storage` — 非会话数据的存储中心（`ctx.storage`）：具名后端注册表加已挂载的数据形式设施 入口 `src/index.ts`（tests 1）
- `packages/storage/storage-domain` — DeepSeek Harness 存储中心的领域数据形式：在所有已配置的后端注册后… 入口 `src/index.ts`（tests 2）
- `packages/storage/storage-json` — 存储中心的 JSON 后端：配置根目录下每个单元使用一个人类可读的 `<unit>.json` 文件，注册为后端 `json` 入口 `src/index.ts`（tests 1）
- `packages/storage/storage-sqlite` — 存储中心的 SQLite 后端：注册为后端 `sqlite`，通过一个数据库提供 `kv` facet… 入口 `src/index.ts`（tests 2）

## packages/subagent

- `packages/subagent/subagent` — subagent seam 允许一个 agent（智能体）通过具名提供方把工作委派给子 agent [client] 入口 `src/index.ts`（tests 10）
- `packages/subagent/subagent-acp` — ACP（Agent Client Protocol）提供方会在全新的子进程中运行每个 subagent… 入口 `src/index.ts`（tests 1）
- `packages/subagent/subagent-claude-code` — 本包（package）注册由 Profile 命名、默认名称为 `claude-code` 的 Claude Code subagent 提供方 [bundle] 入口 `src/index.ts`（tests 2）
- `packages/subagent/subagent-codex` — 本包注册由 Profile 命名、默认名称为 `codex` 的 Codex subagent 提供方 [bundle] 入口 `src/index.ts`（tests 2）
- `packages/subagent/subagent-dsh-sdk` — SDK 提供方会在全新的子进程中把每个 subagent 作为完整的 DeepSeek Harness 运行时运行… 入口 `src/index.ts`（tests 1）
- `packages/subagent/subagent-fork-in-process` — fork 提供方会创建一个进程内子 agent（智能体），并以父 agent 已完成的对话轮次作为初始内容 入口 `src/index.ts`（tests 2）
- `packages/subagent/subagent-in-process-driver` — 本包是两个进程内提供方共用的运行驱动器 入口 `src/index.ts`（tests 4）
- `packages/subagent/subagent-spawn-in-process` — spawn 提供方会在当前进程中创建一个全新的子 `Agent` 入口 `src/index.ts`（tests 1）
- `packages/subagent/tool-subagent` — 基于一个已配置 `ctx.subagents` 提供方、面向模型的委派工具 入口 `src/index.ts`（tests 2）
- `packages/subagent/tool-subagent-control` — 可选的全局具名 `send_message`、`interrupt_agent` 与 `list_agents` 工具是 `ctx.subage… 入口 `src/index.ts`（tests 2）
- `packages/subagent/tool-subagent-report` — 可选的子级作用域 `report` 工具是 `ctx.subagents.reportFrom()` 之上的轻量适配器 入口 `src/index.ts`（tests 1）

## packages/subprocess

- `packages/subprocess/subprocess` — 子进程 seam（`ctx.subprocess`）是一个执行世界的进程部分 入口 `src/index.ts`（tests 2）
- `packages/subprocess/subprocess-local` — `@deepseek-ai/dsh-subprocess` seam 的 Service Provider 入口 `src/index.ts`（tests 11）

## packages/terminal

- `packages/terminal/terminal` — 限定所有者范围的持久 PTY seam 入口 `src/index.ts`（tests 1）
- `packages/terminal/terminal-bash` — 这是一个基于 `ctx.subprocess.spawnTerminal`、为 `ctx.terminals` 提供的持久 shell 后端 入口 `src/index.ts`（tests 5）
- `packages/terminal/tool-terminal` — 基于 `ctx.terminals` 提供 6 个面向模型的工具：`terminal_open`、`terminal_send`、`termin… 入口 `src/index.ts`（tests 3）

## packages/test-support

- `packages/test-support/acp-snapshot` — ACP（Agent Client Protocol）快照套件工具包：无密钥快照层（`pnpm run test:snapshot`… 入口 `src/index.ts`（tests 3）
- `packages/test-support/agent-loop-testkit` — 为运行具体 `AgentLoop` 的测试共享挂载先决依赖 入口 `src/index.ts`（tests 1）
- `packages/test-support/client-runtime` — 面向客户端功能测试的 jsdom slot 测试运行时：真实 Cordis `Context`、生产 `SlotRegistry` 与 UI 渲… 入口 `src/index.ts`（tests 3）
- `packages/test-support/llm-mock-server` — 可编脚本的 OpenAI 兼容 HTTP／SSE（Server-Sent Events）服务器，用于在无提供方密钥的情况下测试真实 LLM… 入口 `src/index.ts`（tests 3）
- `packages/test-support/llm-replay` — 用于无密钥快照测试的 LLM（大语言模型）回放插件 入口 `src/index.ts`（tests 1）
- `packages/test-support/loader-smoke` — 用于测试通过 Cordis Loader 启动应用和 `cordis.yml` 的共享子进程 harness 入口 `src/index.ts`（tests 3）

## packages/todo

- `packages/todo/tool-todo` — 面向模型的 `todo_write` 工具：agent（智能体）的完整任务列表，每次调用都会整体替换 [client] 入口 `src/index.ts`（tests 5）

## packages/typert

- `packages/typert/generator` — TypeScript 项目分析器和模型驱动的 Typert 生成器 入口 `src/index.ts`（tests 8）
- `packages/typert/loader` — 生成的 Typert 产物所用的 Loader 集成，仅支持 Node 入口 `src/index.ts`（tests 1）
- `packages/typert/protocol` — 该包提供不依赖编译器的声明，由业务包、生成的 Typert 产物、宿主网关和客户端 API 共享 入口 `src/index.ts`（tests 1）
- `packages/typert/registry` — 生成的 Typert 产物所用的运行时注册表 [client] 入口 `src/index.ts`、`src/client/index.ts`（tests 1）

## packages/util

- `packages/util/atomic-write` — 零依赖的原子文件替换，供绝不允许在磁盘上留下不完整、被符号链接劫持或权限过宽内容的文件型存储共用：用户设置文档… 入口 `src/index.ts`（tests 2）
- `packages/util/brand` — 品牌使 `SessionId` 和 `CallId` 这样结构相同的字符串在类型层面不可互换，尽管两者在运行时都是普通 `string` 入口 `src/index.ts`
- `packages/util/home-paths` — DeepSeek Harness 用户数据的共享文件系统路径辅助工具 入口 `src/index.ts`（tests 1）
- `packages/util/launch-environment` — 把本次运行的环境冻结为一份不可变快照，并记住**每个值来自哪一层** 入口 `src/index.ts`（tests 1）
- `packages/util/native-command` — 一个**零依赖、免 Shell 的原生命令与路径打开库** 入口 `src/index.ts`（tests 2）
- `packages/util/output-retention` — 一个轻依赖的**保留**库：为必须限制返回上下文量的工具提供有界的面向模型输出 入口 `src/index.ts`（tests 1）
- `packages/util/timeout` — 超时的**时序与分类**部分：一个零依赖纯函数库（无运行时 harness 依赖），由每个需要限制调用方超时提示、启动 deadline… 入口 `src/index.ts`（tests 1）

## packages/web

- `packages/web/tool-web` — 面向模型的 web 工具套件 `web_search` 与 `web_fetch`，构建于 web 能力 seam（`ctx.web`）之上 入口 `src/index.ts`（tests 4）
- `packages/web/web` — **`WebRuntime`**（`ctx.web`）定义 harness 具备哪些 web 访问能力（搜索 web、抓取 URL）… 入口 `src/index.ts`（tests 1）
- `packages/web/web-fetch-http` — 一个匿名公共 HTTP(S) `WebFetchProvider`，用于 harness web 能力 seam（`ctx.web`） 入口 `src/index.ts`（tests 1）
- `packages/web/web-search-deepseek` — 由 DeepSeek 支持的 `WebSearchProvider`，用于 harness web 能力 seam（`ctx.web`） 入口 `src/index.ts`（tests 3）
- `packages/web/web-search-exa` — 由 Exa 支持的 `WebSearchProvider`，用于 harness web 能力 seam（`ctx.web`） 入口 `src/index.ts`（tests 1）
- `packages/web/web-search-perplexity` — 由 Perplexity 支持的 `WebSearchProvider`，用于 harness web 能力 seam（`ctx.web`） 入口 `src/index.ts`（tests 1）

## packages/workflow

- `packages/workflow/tool-ralph` — 面向模型的 `ralph` 工具运行固定的前台工作流，把一个不可变目标依次交给多个全新子 agent（智能体） 入口 `src/index.ts`（tests 2）
- `packages/workflow/tool-workflow` — 面向模型的 **`workflow` 工具**：运行一段扇出 subagent 的 JavaScript 编排脚本，并返回脚本的最终值 入口 `src/index.ts`（tests 2）
- `packages/workflow/workflow` — 工作流 seam（扩展点，`ctx.workflowEngine`）执行由模型编写、可扇出 subagent 的编排脚本 入口 `src/index.ts`（tests 2）
- `packages/workflow/workflow-worker-thread` — 本包为 `WorkflowEngine` 提供实现，每次运行使用一个 Node worker thread 入口 `src/index.ts`（tests 6）

## packages/workspace

- `packages/workspace/workspace` — DeepSeek Harness 的 Workspace 实体注册表（`ctx.workspaceRegistry`）：通过领域数据形式存储持久… 入口 `src/index.ts`（tests 2）
